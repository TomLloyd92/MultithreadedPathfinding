#include "Enemy.h"

Enemy::Enemy()
{
}

Enemy::~Enemy()
{
}

void Enemy::update(sf::Time t_deltaTime)
{
}

void Enemy::render(sf::RenderWindow& t_window)
{
}

void Enemy::setup(sf::Font& t_font)
{
}

void Enemy::initialise()
{
}

void Enemy::currentNode()
{
	//m_currentNode = floor((pos.x + (TILE_WIDTH / 2)) / TILE_WIDTH) + (floor((pos.y + (TILE_WIDTH / 2)) / TILE_WIDTH) * Rows);
}
